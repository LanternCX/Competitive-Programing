#include <bits/stdc++.h>

using namespace std;

void sol() {
    cout << "Building the Future, One Line of Code at a Time." << '\n';
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);

    int t = 1;
    // cin >> t;
    while (t--) {
        sol();
    }
    return 0;
}