#include <bits/stdc++.h>
using namespace std;
void sol(){
    string s;
    cin >> s;
    cout << s + ", start!\n";
}
int main(){
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int n;
    cin >> n;
    while(n--){
        sol();
    }
    return 0;
}