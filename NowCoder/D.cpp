#include <bits/stdc++.h>
using namespace std;
using ll = long long;
void sol(){
    ll n, m;
    cin >> n >> m;
    string a, b;
    cin >> a >> b;
    
}
int main(){
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    ll t;
    cin >> t;
    while(t--){
        sol();
    }
    return 0;
}