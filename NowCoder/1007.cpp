#include <bits/stdc++.h>
using namespace std;
int main(){
    // ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int a, b;
    cin >> a >> b;
    printf("%.3lf%%", b * 1.0 / a * 100);
    return 0;
}