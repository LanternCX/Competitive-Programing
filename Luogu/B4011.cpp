#include <bits/stdc++.h>
using namespace std;
const int N = 1009;
int an[N];
int bn[N];
int main(){
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int m, n;
    cin >> m >> n;
    for(int i = 0; i < m; i++){
        cin >> an[i];
    }
    for(int i = 0; i < m; i++){
        cin >> bn[i];
    }
    
    return 0;
}